# 09:00 아침 일상 글 "자동 복구 실패" 슬랙 — 원인과 조치 (2026-09-24 09:53)

## 결과 먼저
- 아침 일상 글 발행(작업 195)은 **09:52에 시작해 지금 진행 중**입니다(52분 지연, 실패 아님). 발행 창은 09:00에서 18:00이라 오늘 물량은 모두 나갑니다.

## 원인 — 옛 OS 예약 작업이 실행기 자리를 가로챔
1. 08:30 Windows 작업 스케줄러의 옛 예약 **V2R-Reconcile**(실행기 내장 예약이 생기기 전 방식)이 별도 프로세스로 "끊긴 작업 점검"을 띄웠습니다.
2. 그 프로세스가 실행기 리스(한 번에 하나만 작업을 돌리는 자물쇠)를 잡고, 대기열에 있던 **연관도 재채점(작업 189, 몇 시간짜리)** 을 자기 안에서 돌리기 시작해 리스를 계속 연장했습니다.
3. 그래서 09:00 발행은 대기열에서 못 나왔고, 예약 감시견이 "재시도 → 자동 복구 실패"를 슬랙에 보냈습니다. 08:05 시트 키워드 반영(186)도 같은 이유로 시트 잠금 대기 초과로 실패했습니다.

## 조치
- 09:50 그 프로세스의 리스를 회수해 실행기가 발행을 시작(취소한 재채점은 발행이 끝난 뒤 다시 실행, 이미 채점된 것은 건너뜀).
- 옛 예약 스크립트(reconcile.cmd)를 즉시 종료하도록 무력화(커밋). **작업 스케줄러의 V2R-Reconcile 비활성화는 제 권한으로 막혀 있어(시스템 설정 변경) 직접 꺼 주시면 확실합니다** — 안 꺼도 스크립트가 바로 끝나므로 재발은 없습니다.
- 예약 감시견 오경보(대기열에 서 있는 걸 실패로 봄)는 앞서 커밋(6f61c9e)으로 고쳤고, 재채점처럼 긴 작업은 발행과 같은 줄에 서지 않도록 **별도 줄(light)로 옮기는 수정**을 이어서 합니다.
- 시트 키워드 반영은 11:30 자동 예약에서 다시 붙습니다(팥순이 2,537·코숨핏 307·뉴더미스 14).
