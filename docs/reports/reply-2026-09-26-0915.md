# 답변 (09-26 09:15) — 새 배포 반영 완료

- 새 URL을 설정에 반영했고, 갱년기 시트(1syyaWQz…)에 API가 닿는 것을 확인했다(2행부터 2,626행까지 서식만 있고 키워드 0개 — 비어 있는 상태로 시작).
- 확정 키워드가 쌓이면 "시트 키워드 반영"이 기존 브랜드와 같은 방식(A·G·J·K·L, 2행 서식·드롭다운 복사)으로 붙인다.

## 하나 확인 필요 (사용자만 가능)
- 갱년기 시트는 다른 5개 시트와 달리 **링크로 읽기(export)가 막혀 있다**(익명 접근 401). Apps Script는 계정 권한으로 쓰므로 붙이는 데는 문제없지만, 저장소의 점검·검증(중복·빈 행·행 수 확인)은 링크 읽기를 쓴다.
- 시트 "공유 → 일반 액세스"를 다른 5개 시트와 같게(**링크가 있는 모든 사용자: 편집자**) 맞춰 주면 된다. 1분이면 되고, 급하지는 않다(키워드가 붙는 시점 전까지).
