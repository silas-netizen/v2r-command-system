# 병렬 노출 확인(시간당 800건 이상)의 부담·리스크 점검 (2026-09-23 23:58)

## 이 PC 자원 (실측)

| 항목 | 값 |
|---|---|
| 메모리 | 총 23.9GB, 지금 여유 9.6GB |
| CPU | 논리 16코어, 지금 부하 47% |
| 현재 크롬 프로세스 | 26개(약 750MB) — 상주 크롬·순환기 브라우저 등 |
| 실행기 파이썬 | 약 225MB |

## 리스크와 대책

| 리스크 | 정도 | 대책 |
|---|---|---|
| **네이버 차단(IP 단위)** — 가장 큰 리스크. 한 IP에서 분당 15~20회 통검 조회 | 높음 | 작업자별 6~10초 무작위 간격, 자동완성·글 열기는 같은 세션 재사용으로 요청 수 최소화, 미확인 연속 3건이면 그 작업자 15분 휴식, 전체 차단 징후면 30분 정지. **처음엔 2개 병렬로 시작해 1시간 실측 후 3→4→5로 올림**. 차단 시 자동 감속 |
| PC 메모리·CPU | 낮음 | 헤드리스 크롬 1개 ≈ 200~300MB, 5개면 약 1.5GB·CPU 2~3코어. 여유 9.6GB·16코어라 문제 없음. 발행기(09~18시)와 겹칠 때는 병렬 3개로 자동 축소 |
| 발행 지연 간섭 | 중간 | 노출 확인은 사이드카가 아닌 별도 숨김 프로세스라 발행 루프를 막지 않음. 네이버 계정 프로필은 브랜드별 사본(`kw-<브랜드>`)만 쓰고 발행용 프로필은 건드리지 않음 |
| 구글 시트 쓰기 폭주 | 중간 | 지금처럼 20건/5분 묶음 유지, 브랜드별 잠금 파일로 동시 쓰기 방지. 시트 쓰기가 밀려도 DB에는 즉시 기록되어 손실 없음 |
| 실행기 DB 잠금 충돌 | 낮음 | SQLite WAL + 짧은 쓰기. 작업자는 DB에 결과만 append, 사이드카는 읽기 위주 |
| 네이버 로그인 세션 소모 | 낮음 | 통검·카페 글 읽기는 로그인 없이도 되므로 익명 컨텍스트 사용, 프로필 쿠키는 필요 시만 |
| 판정 품질 저하(속도 우선으로 스크롤 조기 종료) | 중간 | "카페 카드 영역이 로드되고 높이가 2번 연속 같을 때"만 중단, 카페 카드가 하나도 없으면 기존처럼 끝까지 스크롤. 하루 30건 표본을 GPT 교차 판정으로 계속 검증 |

## 결론

PC 부담은 작고, 실제 리스크는 **네이버 차단 하나**입니다. 그래서 병렬 수는 2개부터 단계적으로 올리고, 차단 징후가 보이면 자동으로 줄이도록 설계합니다. 우선순위 재검사로 하루 필요량을 1~2만 건으로 줄여 5개까지 올릴 필요가 없을 수도 있습니다.
