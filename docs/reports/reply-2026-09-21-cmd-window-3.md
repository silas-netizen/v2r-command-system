# "접근할 수 없다" 메시지의 정체 (2026-09-21 09:00 KST)

## 오류 메시지 맞습니다. 다만 "지나간" 오류입니다.
- 뜻: "다른 프로그램이 그 파일을 쓰고 있어서 나는 못 쓴다."
- 그 파일 = 실행기 로그(`logs\serve.log`). 실행기 껍데기(cmd 창)는 30초마다 "로그에 한 줄 적기 → 파이썬 실행기 켜기"를 반복하는데, **껍데기가 두 개** 동시에 살아 있으면 같은 로그 파일을 서로 붙잡아 진 쪽이 저 한 줄을 찍고 30초 뒤 다시 시도합니다.
- 두 개가 된 이유: 09-19 새벽에 켜진 옛 껍데기가 안 죽고 남아 있었고, 오늘 02:30~02:55에 제가 실행기를 세 번 새로 띄우면서 겹쳤습니다. 그 사이에 찍힌 줄들이 화면에 그대로 남아 있는 것입니다.

## 지금은 멈췄나
- 네. 옛 껍데기는 아까 종료했고, 지금 창의 껍데기는 파이썬 실행기를 켠 채 그 안에서 기다리는 상태라 새 줄을 찍을 수 없습니다(파이썬이 끝나기 전까지 화면이 바뀌지 않음). 화면에 보이는 줄은 전부 과거 것.
- 로그 파일도 정상적으로 계속 쓰이고 있고(마지막 기록 02:55 실행기 시작), 발행은 08:00부터 문제없이 진행 중입니다.

## 정리
- 창 내용 = 과거 오류의 흔적, 현재 상태 아님. 최소화해 두시면 되고, 오늘 발행이 끝난 뒤 재시작하면 이 창은 사라지고 이후로는 창 자체가 뜨지 않습니다.
- 같은 일이 다시 생기지 않도록 재시작 스크립트가 껍데기를 반드시 1개만 남기게 해 두었습니다.
