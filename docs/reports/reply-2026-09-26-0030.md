# 답변 (09-26 00:30)

## 1. 색
- 밀려남을 회색으로 바꿨다. 최종: 노출완 초록(#b7e1cd) · 밀려남 회색(#efefef) · 미확인 회색(#efefef). A열 카페 색은 그대로.
- 배포되면 자동으로 5개 시트에 적용된다.

## 2. 배포 방법 (3분)
1. 코드 복사: [copy_v2r_sheet_api.html](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/docs/appsscript/copy_v2r_sheet_api.html) 열고 **"코드 전체 복사"** 버튼.
2. https://script.google.com/home 에서 프로젝트 열기 → 편집창 클릭 → **Ctrl+A, Delete, Ctrl+V** → **Ctrl+S**.
3. 오른쪽 위 **배포 → 배포 관리 → 연필 → 버전: 새 버전 → 배포**.
4. 화면에 뜨는 웹앱 URL이 지금 것(…RkhJY8/exec)과 다르면 그 URL을 채팅에 보내 주면 된다. 같으면 끝.

## 3. 갱신 파일 규칙
- 앞으로 답변 파일은 새 내용만 쓴다(이전 내용 누적 안 함). 이 파일부터 적용.
