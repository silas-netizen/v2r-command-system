# 노출 확인에 어떤 모델이 맞나? (2026-09-24 02:05)

**결론: 노출 확인 자체에는 모델(LLM)을 쓰지 않는 게 가장 빠르고 정확합니다.** 지금도 안 씁니다.

## 왜
노출 확인은 "판단"이 아니라 "확인"입니다.
1. 통검 첫 페이지를 끝까지 내려 카페 카드를 찾고,
2. 우리 제휴·자사 카페의 대표 글인지 링크로 확인하고,
3. 그 글의 댓글2 계열에 브랜드 식별어 문자열이 있는지 봅니다.

세 단계 모두 규칙이 고정된 문자열·구조 비교라 코드(파서)가 0.1초에, 100% 같은 답으로 처리합니다. 모델을 끼우면 건당 수 초가 더 들고, 같은 입력에 답이 흔들릴 수 있어 오히려 정확도가 떨어집니다. 속도의 병목은 모델이 아니라 **네이버 페이지 로딩·스크롤(건당 약 30초)** 이고, 그래서 상주 브라우저·조기 종료·병렬 작업자로 줄이는 중입니다.

## 모델이 필요한 곳(보조)
- **판정 검증(감사)**: 파서가 맞게 봤는지 표본을 사람 대신 읽어 대조 — 지금처럼 GPT 5.6-sol 교차검증(아침 브랜드별 10개). 여기는 정확도가 중요하니 상위 모델이 맞습니다.
- **파서가 못 읽는 예외**(페이지 구조가 바뀐 경우)를 알아채 알리는 용도 — 드물게, 작은 모델로 충분.

## "오푸스 5.5"
지금 쓸 수 있는 클로드는 Fable 5.1(이 세션)·Opus 5·Sonnet 5·Haiku 4.5입니다. 5.5는 없습니다. 감사·검증용으로는 Opus 5 또는 GPT 5.6-sol, 판정 자체는 모델 없이 파서 — 이 조합을 유지하겠습니다.
