# 자동 복원 허용 → 구현 완료 (2026-09-21 15:05 KST)

## 넣은 것 (테스트 950 통과, GitHub 반영)
| 단계 | 동작 |
|---|---|
| 점검(매일 09:25 + 수동 `요금제 세션 점검`) | `claude auth status`로 로그인 확인(모델 호출 0) |
| 정상이면 | 자격증명 파일을 `data/claude-cli-backup/`에 자동 백업(내용 같으면 건너뜀, 최근 3개 보관) |
| 풀렸으면 | **최신 백업을 자동 복원 → 다시 확인**. 성공하면 사람에게 알리지 않고 기록만 남김 |
| 그래도 안 되면 | 그때만 텔레그램 알림 + 원클릭 재로그인 스크립트 안내(최후 수단) |
- 방금 실제 실행: 로그인 정상, 첫 자동 백업 생성 확인.
- 파일 내용은 읽거나 출력하지 않고 바이트 복사만 합니다(비밀번호 아님, 1회용 승인으로 받은 토큰 파일).

## 같은 원칙이 적용되는 곳(현재)
V2R 토큰 갱신 / ChatGPT 프로필 / 네이버(하루 4회 연장·백업·복구) / Claude 플랫폼·Make(구글, 프로필 백업·복구) / Claude Code 명령줄(자격증명 백업·복원). 모두 "풀리면 스스로 복구 → 안 되면 알림".

## 참고
- 실행기는 14:27에 재시작한 코드라 이 복원 기능은 다음 재시작(오늘 발행 끝난 뒤) 후 예약 점검에 반영됩니다. 그 전엔 제가 직접 돌려 확인했습니다.
- 규칙(허용 포함)은 기억 파일에 저장했습니다.
