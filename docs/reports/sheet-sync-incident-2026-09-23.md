# 시트 키워드 반영 사고 보고 — 팥순이 탭 빈 행 3,023개 (2026-09-23 16:57)

## 결론

- 원고 대상 키워드를 브랜드 시트에 넣는 작업 중 **팥순이 시트 "노출 현황" 탭 2~3,024행에 빈 행 3,023개가 끼어 들어갔습니다.** 데이터는 지워지지 않았고 아래로 밀려 있을 뿐입니다(3,025행부터 원래 데이터).
- 덮어쓴 행 1개(유산균다이어트)는 로컬 백업으로 **복구 완료**했습니다.
- 빈 행 삭제는 제 자동 모드가 "공유 자원 수정"으로 막아 **실행하지 못했습니다.** 승인해 주시거나 직접 지워 주시면 됩니다(아래 3).

## 1. 무슨 일이 있었나

| 시각 | 내용 |
|---|---|
| 16:11~16:20 | 5개 브랜드 시트에 키워드 추가 시도. 장으뜸 283·뉴더미스 451·우아덤 181건은 정상 추가. 코숨핏·팥순이는 0건 |
| 원인 1 | 코숨핏(449행)·팥순이(3,654행) 탭은 **격자가 데이터 크기로 잘려 있어** 그 아래 셀로 이동하면 "지정한 범위가 시트 크기를 초과합니다" 창이 뜨고 붙여넣기가 안 됨 |
| 원인 2 | 검증이 시트의 숫자 서식(`1,573,435`)을 다르게 봐서 실제로는 잘 써진 P1/Q1도 "불일치"로 찍힘(하루 1,098회 헛재시도) → 정규화로 해결 |
| 16:5x | 격자를 늘리는 코드를 넣고 팥순이에서 시험. **현재 행 수를 Ctrl+End로 어림한 것이 1행으로 잘못 읽혀** 1행 아래부터 1·2·4·…·1,000행씩 총 3,023개 빈 행을 끼워 넣음. 그 뒤 3,655행에 쓴 시험 행 1개가 원래 데이터(유산균다이어트)를 덮음 |
| 16:5x | 덮인 행은 9-19 로컬 엑셀 백업의 같은 행으로 복구(확인 완료). 격자 크기는 이제 이름 상자 이동으로 **정확히 이진 탐색**해서 잰 뒤 마지막 행 아래에만 추가하도록 고침 |

현재 5개 탭 전체를 CSV로 백업해 두었습니다: [data/sheet_backups/](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/data/sheet_backups/)

## 2. 지금 시트 상태

| 브랜드 | 탭 | 상태 |
|---|---|---|
| 팥순이 | 노출 현황 | **2~3,024행 빈 행** (삭제 필요). 3,025행부터 원래 데이터. 덮인 행 복구됨. A1 헤더가 "카페" 대신 통검 URL로 되어 있음(이전 사고 흔적, 고쳐야 함) |
| 코숨핏 | 노출 현황의 사본 | 변화 없음(449행). 탭 이름이 "…의 사본"인데 이 탭이 맞는지 확인 필요 |
| 우아덤 | 노출 현황 | 키워드 181건 추가됨. A1 헤더가 비어 있음(고쳐야 함) |
| 장으뜸 | 노출 현황 | 키워드 283건 추가됨. A1에 "485765"(P1 합계가 잘못 들어간 이전 흔적, 고쳐야 함) |
| 뉴더미스 | 노출 현황 | 키워드 451건 추가됨. 정상 |

A1 오염 3건은 이름 상자 이동이 씹힌 채 붙여넣기가 A1에 떨어진 이전 사고로, 이제 이동 확인 후에만 붙여넣도록 막았습니다.

## 3. 필요한 조치 (승인 요청)

1. **팥순이 2~3,024행 삭제**: "승인"이라고 답해 주시면 제가 창 없이 지웁니다(지우기 전에 그 행들이 전부 빈 행인지 다시 확인). 또는 직접: 팥순이 시트 노출 현황 탭에서 행 2~3024 선택 → 우클릭 → 선택한 행 삭제.
2. A1 헤더 3건을 "카페"로 복구(승인 시 같이 진행).
3. 그 뒤 키워드 반영을 다시 실행(격자 자동 확장 + 200행 단위 + 검증 정규화 적용). 대상: 팥순이 2,757건·코숨핏 363건·우아덤 나머지 약 1,020건.

## 4. 재발 방지 (코드 반영·커밋 완료)

- 격자 크기 이진 탐색 후 마지막 행 아래에만 추가 → [sheets_writer.py](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/sources/sheets_writer.py)
- 이름 상자 이동 확인 없이는 붙여넣기 금지, 모달이 뜨면 닫고 실패 처리
- 검증 정규화(콤마·소수), 200행 단위 붙여넣기
- 시트 쓰기 전 탭 전체 CSV 백업을 자동으로 남기도록 다음 단계에서 추가 예정
