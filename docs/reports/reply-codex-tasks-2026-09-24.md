# Codex(GPT)가 필요한 업무와 내일 리셋 시 계획 (2026-09-24 21:45)

## Codex를 쓰는 업무 3가지
| 업무 | 무엇을 하나 | 모델 | 지금 대기량 |
|---|---|---|---|
| **1. 키워드 연관도 교차검증** | 클로드가 매긴 0에서 4 점수를 GPT가 독립 채점, 둘 다 통과해야 원고 대상 확정 | gpt-6-astra(low), 대체 gpt-5.6-sol | **30,074개**(팥순이 11,613·장으뜸 8,589·우아덤 5,216·코숨핏 3,021·뉴더미스 1,635) — 50개 묶음 ≈ 600회 호출 |
| 2. 브랜드 원고 교차검증 | 생성된 브랜드 원고를 GPT가 규칙·품질 점검(60점 미만 재작성) | gpt-5.6-sol(medium) | 대량 원고 시작 전까지 0 |
| 3. 노출 판정 감사 | 러너 판정 브랜드별 10개를 GPT가 독립 재판정(아침 보고) | gpt-5.6-sol(medium) | 하루 50개, 50회 호출 |

일상 글·댓글·발행·노출 확인 자체·시트 갱신은 Codex를 쓰지 않습니다.

## 내일 리셋되면
- 자동 보류(재개 시각 파일)를 리셋 직후 제가 해제하면 재채점 병렬 프로세스 5개가 대기 30,074개 교차검증을 바로 시작합니다. 묶음 50개·브랜드 병렬 기준 **한도 소모를 줄이려면** 검색량 큰 키워드부터, 당위성(3) 후보(9,154개)를 먼저 검증하도록 순서를 두겠습니다(이게 원고 대상 증가에 직결).
- 하루 한도로 30,074개를 다 못 볼 수 있으니: 우선순위 순서 + 한도 문구 감지 시 자동 보류 → 다음 리셋에 이어서. 진행 수는 현황판에 브랜드별로 갱신합니다.
- 리셋 뒤 "코덱스 리셋했어"라고만 알려 주시면 보류를 즉시 풉니다(자동 감지도 재개 시각 지나면 풀림).
