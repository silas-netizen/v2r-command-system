# 답 3건 (2026-09-23 02:08 KST, 실측)

1. **가상 PC 내보내기 형식** — 인수인계 문서 기준으로 확정해 구현 중: ① 5개 브랜드 시트 `게시글 쓰기 원본` 탭 형식(A 키워드 · B 본문+댓글 12블록 · D 작성계정 · E 원고유형 · F 완료 링크)으로 원고 행 추가·xlsx 내보내기, ② 댓글 프로그램용 3열 파일(작성자 구분/탐지할 댓글/댓글 내용, 행 순서·계정 배정표·`(테스트 입니다.)` 제거·`댓글_YYMMDD.xlsx`+CSV 폴더) 그대로. 명령 `가상pc 원고 내보내기`.
2. **형식적 키워드 제외 + Astra 교차 검증** — 당위성 없는 일반어(진료과·지역·생활어)는 무관(3)으로 두어 원고 대상 제외. Claude 점수와 **GPT Astra(low)** 점수를 같은 기준으로 매겨 2점 이상 차이는 검토 표시·보수적으로 채택, 둘 다 0~2인 키워드만 원고 대상. 브랜드별 일치율을 보고.
3. **시트에는 아직 안 적었습니다.** 시트에 쓰려면 구글 승인 1회가 필요한데 그 화면(크롬 창의 "Google hasn't verified this app" → Advanced → Go to V2R 시트 쓰기 → 허용)은 보안 규칙상 제가 못 넘깁니다. 승인해 주시면 5만 개 키워드(연관도·당위성·검색량 포함)를 두 번째 탭에 한꺼번에 넣고, 이후 노출 순환이 G/J/L열과 P1/Q1을 실시간 갱신합니다. 그 전까지는 로컬 DB·CSV(`data/keywords/`, `data/exposure/`)에만 있습니다.
