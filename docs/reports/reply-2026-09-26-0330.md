# 답변 (09-26 03:30) — 색 통일, 남은 것 하나

## 확인한 것
- 색 규칙은 5개 시트 모두 같은 13개다(03:00 재적용, 값과 색 동일). 색은 더 손댈 것이 없다.
- 시트마다 달라 보이는 유일한 원인은 드롭다운 **표시 스타일(칩 / 화살표)** 이다. 이 설정은 구글이 API·Apps Script 어느 쪽으로도 열어 주지 않아 프로그램으로는 못 바꾼다.
- 03:15에 앱 안 브라우저로 직접 바꿔 보려 했으나 시트 화면이 작게 잡혀 클릭이 엉뚱한 메뉴(글꼴·셀 드롭다운)로 들어갔다. 값이 바뀌지 않은 것은 export로 확인했고(코숨핏 2에서 5행 그대로) 즉시 중단했다. 9월 25일 시트 사고와 같은 경로라 다시 시도하지 않는다.

## 부탁 (시트당 2번, 총 10번, 3분)
1. 시트에서 A열 머리글 클릭 → 메뉴 **데이터 → 데이터 확인**.
2. 오른쪽 패널의 규칙 클릭 → **고급 옵션** 펼침 → **표시 스타일: 칩** → **완료**.
3. G열도 같은 방법. 5개 시트 반복.

끝나면 5개 시트가 두 번째 사진처럼 같은 칩 색으로 보인다. 확인 요청만 주면 제가 5개 시트 색 규칙을 한 번 더 점검한다.
