# 재시작 점검에서 잡은 결함 — "기존 노출완"이 1순위로 안 뽑히던 문제 (2026-09-24 02:30, 커밋 0fb0d3d)

## 발견
02:19 재시작 직후 실제로 뽑힌 키워드를 대조해 보니 코숨핏은 "이비인후과·정형외과"(검색량 큰 밀려남)부터 갔고, 시트 노출완 키워드는 하나도 먼저 오지 않았습니다.

## 원인 두 가지
1. 시트 상태 열을 읽는 헤더 이름이 옛 이름("검색노출"·"T0")이라 "노출 상태" 열이 항상 빈 값으로 들어옴 → 1순위 판정이 한 번도 참이 되지 않음.
2. 검사 대상 목록을 만들 때 **09-19에 받아 둔 xlsx 스냅샷**을 실시간 시트보다 먼저 읽음 → 그 뒤 바뀐 G열 값(노출완/밀려남)이 반영 안 됨.

## 수정
- G열 "노출 상태"를 그대로 읽어 1순위 판단에 사용.
- 대상 목록은 **항상 실시간 시트**(export CSV, 필터 무시)를 먼저 읽고, 실패할 때만 gviz → xlsx 순으로 대체.
- 검증(코드에서 직접 실행): 팥순이 1순위 545건, 코숨핏 1순위 5건(나머지 노출완은 지난 1시간 안에 이미 검사됨) → 다음 10건이 전부 시트 노출완 키워드로 뽑힘.
- 러너 즉시 재시작 지시(작업자 3개). 시험 104개 통과.

## 덧붙여
코숨핏 시트에는 같은 키워드가 두 번 이상 적힌 행이 194개 있습니다(449행 중 고유 키워드 254개). 검사는 키워드당 한 번만 하고 결과는 같은 키워드의 모든 행에 씁니다. 중복 행 정리는 지시 주시면 하겠습니다.
