# "GPT 검증"은 무엇인가 (2026-09-24 10:40)

네, 모델입니다. 키워드 연관도를 **두 모델이 독립적으로 채점**하고 둘 다 통과해야 원고 대상으로 확정합니다.

| 역할 | 모델 | 경로·비용 |
|---|---|---|
| 1차 채점 | 클로드(요금제 길, Claude Code CLI) | 0원 |
| 교차검증("GPT 검증") | **OpenAI gpt-6-astra**(low) — 안 되면 gpt-5.6-sol로 대체 | Codex CLI(로그인 유지), 창 없음 |

- 같은 채점 기준(0 직접·1 근접·2 확장·3 당위성·4 무관, 브랜드별 예시)을 두 모델에 주고, **둘 다 0에서 3**(3은 당위성 논리가 저장된 것만)이어야 확정. 한쪽이라도 4(무관)면 제외, 한쪽이 아직 안 봤으면 "미검증"으로 보류.
- 노출 확인 결과 감사(브랜드별 10개 대조)에는 정확도가 더 필요해 gpt-5.6-sol(medium)을 씁니다.
- 원고 작성은 클로드가 하고, GPT는 채점·검증에만 씁니다.
