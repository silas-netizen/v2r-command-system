# 요금제(Max 20x)로 브랜드별 1건 재생성 — 일관성 유지 방법 (2026-09-21 16:40 KST)

## 먼저 1회 필요한 것: Claude Code 명령줄 로그인
- 요금제 길은 Claude Code의 **명령줄 실행**(`claude -p`)을 씁니다. 이 앱(데스크톱)은 로그인돼 있지만, 명령줄은 **별도 로그인**이 필요합니다(지금 "Not logged in").
- 방법(1분, 딱 한 번): PC에서 터미널(PowerShell)을 열고 아래 실행 → 대화창이 뜨면 `/login` 입력 → 브라우저에서 같은 Anthropic 계정(Max)으로 승인 → 터미널 닫기.
```powershell
& "$env:LOCALAPPDATA\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude\claude-code\2.1.275\claude.exe"
```
- 이 로그인은 PC에 저장돼 계속 유지됩니다(다시 열게 하지 않음). 끝나면 "로그인 했어"라고 알려 주세요.

## 일관성 유지 — 무엇을 어떻게 보장하나
| 보장 항목 | 방법 | 확인 수단 |
|---|---|---|
| **지침·예시가 글자 하나 안 바뀜** | 프롬프트는 `brand_writer` 한 곳에서만 만들고, 요금제 길은 그 문자열을 그대로 파일·표준입력으로 전달. 백엔드는 프롬프트를 수정 못 함 | 본문·댓글 프롬프트의 **SHA-256 해시**를 원고 JSON·검토 MD에 기록. API 길로 만든 오늘 6건과 해시가 같으면 입력이 동일하다는 증명 |
| **모델 동일** | 요금제 길에서도 `--model sonnet`(Sonnet 5) 명시. 자동 변경 없음 | 원고 JSON에 모델명 기록 |
| **하네스 영향 차단** | `--bare`(훅·플러그인·LSP 없음), 도구 전부 금지, 세션 저장 안 함, 별도 작업 폴더(CLAUDE.md·기억 안 붙음), 시스템 프롬프트를 우리 것으로 **교체**(추가 아님) | 테스트로 명령줄 옵션 고정 |
| **검증 동일** | 같은 `validate`(줄 나눔·{키워드} 위치·대대댓글2 금칙어·필수 멘트·길이·이모지) + 같은 부분 재시도 | 검토 MD의 검증 표 |
| **예시 동일** | 기존 원고 예시는 시트 앞에서부터 고정 선택 | 해시로 포함 |
| **품질 비교** | 오늘 API로 만든 6건 옆에 요금제로 만든 6건을 같은 채점표(100점)로 나란히 | `brand-test-plan-2026-09-21.md`(예정) |
| **어느 길로 만들었는지 항상 표시** | JSON·검토 MD·아침 보고에 `backend: plan/api`, 비용 0원 | — |
| **한도 대응** | 요금제 한도에 걸리면 5시간 잠그고 API로 자동 전환(그 원고에 표시). 이 채팅과 한도를 나누므로 대량 생성은 새벽 02:00~08:00 | 이벤트 로그 |

## 진행 순서
1. (지금) 요금제 길 코드 구현·테스트 — 일꾼 진행 중.
2. 사용자 명령줄 로그인 1회.
3. 브랜드별 1건(팥순이는 질문형·후기형) 요금제로 생성 → 검증 → API판과 나란히 비교 보고서 + 프롬프트 해시 일치 표.
