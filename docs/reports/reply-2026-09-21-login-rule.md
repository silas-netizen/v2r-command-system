# 철칙 저장: 로그인은 1회, 이후 절대 유지 (2026-09-21 16:55 KST)

## 저장한 규칙(기억 파일 + 운영 문서 최상단)
- 모든 서비스의 로그인은 사용자가 **최초 1회**만. 이후는 프로그램이 세션 저장·연장·백업·복구로 유지.
- 재로그인 요청은 자동 복구 4단계(저장 세션 재사용 → 백업 복구 → 토큰 갱신 → 다른 자격증명 경로)가 전부 실패한 뒤 **최후 수단**.
- 새 서비스를 붙일 때는 유지 장치(점검·연장·백업·복구·알림)를 반드시 함께 만든다. 유지 장치 없는 "로그인해 주세요" 구현 금지.
- 비밀번호는 여전히 프로그램이 입력·저장하지 않음(자동화 감지 우회도 안 함).

## 지금 유지 중인 것
| 서비스 | 유지 장치 | 상태 |
|---|---|---|
| V2R | API 토큰 자동 갱신, 로그인 예산 | 로그인 0회 2일째 |
| ChatGPT | 전용 프로필 + 매일 점검 | OK |
| 네이버 | 전용 프로필 + 하루 4회 연장·백업·복구·UA 고정 | OK(30일) |
| Claude 플랫폼(구글) | 진짜 크롬 1회 로그인 프로필 + 매일 점검·백업·복구 | OK(오늘 저장) |
| Make(구글) | 위와 동일 장치 준비됨 | 1회 로그인 전(급하지 않음) |
| Claude Code 명령줄(요금제 실행) | **자격증명 백업·복원·매일 점검 장치를 구현 중**(일꾼에 방금 추가 지시) | 1회 로그인 필요(아래) |

## 마지막 1회
Claude Code 명령줄만 아직 로그인 전입니다. 이건 데스크톱 앱과 별개 저장소라 제가 대신 못 하고, 딱 한 번 필요합니다(이후는 백업·복원으로 유지):
```powershell
& "$env:LOCALAPPDATA\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude\claude-code\2.1.275\claude.exe"
```
→ `/login` → 브라우저 승인 → 창 닫기. 끝나면 "했어".
