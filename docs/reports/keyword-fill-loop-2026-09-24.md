# 키워드 채우기 순환 — 원고 대상 1만 개까지 (2026-09-24)

작성 시각: 2026-09-24 00:26 (KST, `date` 명령 실측)

## 1. 현재 원고 대상 수 (작업 시작 시점, 발굴 1만 개 기준)

원고 대상 = `relevance_llm` 0–2 AND `relevance_codex` 0–2 AND `needs_review` 아님
(`data/keywords/<브랜드>.sqlite`).

| 브랜드 | 원고 대상 | DB 총 키워드(발굴분) |
|---|---|---|
| 우아덤 | 1,203 | 10,000 |
| 장으뜸 | 283 | 10,000 |
| 팥순이 | 3,447 | 10,000 |
| 뉴더미스 | 474 | 10,000 |
| 코숨핏 | 363 | 10,000 |

## 2. 설계

새 모듈 [file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/knowledge/keyword_fill_loop.py](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/knowledge/keyword_fill_loop.py)에
한 회차(`run_cycle`)와 반복(`fill_until_target`)을 넣었다.

- **시드 선택** (`select_seed_keywords`): 아직 시드로 안 쓴(`seeded_at` 빈 값) 원고
  대상 키워드 중 검색량(`total`) 상위 50개. `keywords` 표에 `seeded_at` 열을
  새로 더했다(`migrate_fill_columns`).
- **정리본 보충** (`guide_seed_terms`): 원고 대상 시드가 50개에 못 미치면
  `warehouse/guides/정리본/<브랜드>.md`에서 클로드(요금제 길)로 제품·증상·타깃
  용어를 최대 30개 뽑아 채운다. LLM이 실패하면 기존 정규식 추출
  (`naver_keyword_tool.extract_guide_keywords`)로 대체한다(네트워크 없이도 항상
  동작).
- **수집**: 네이버 키워드 도구(`naver_keyword_tool.fetch_related_keywords`)로
  시드 5개씩 묶어 조회(기존 값 그대로, 조회 간 3–6초 대기·세션 200회당 휴식 등
  차단 방지 로직 재사용). 새 키워드만 `INSERT OR IGNORE`로 저장.
- **채점·교차검증**: 새로 들어온 키워드는 `scored_at`이 비어 있어
  `keyword_relevance.score_brand`(클로드, 요금제 길, 0원)와
  `crosscheck_brand`(Codex `gpt-6-astra`, low)가 자동으로 그 키워드들만 집는다
  — 이미 채점된 기존 키워드는 건드리지 않는다.
- **진행 기록** (`data/keywords/fill_progress.json`): 회차마다 브랜드별
  `eligible`(누적 원고 대상)·`new_this_round`(신규 수집)·`adopted_this_round`
  (신규 중 0–2로 채택된 수)·`adoption_rate`(채택률)·`seed_exhausted`·`status`를
  기록.
- **정지 조건**: 3회차 연속 채택률 5% 미만이면 그 브랜드를 `시드고갈`로 표시하고
  멈춘다(`LOW_ADOPTION_STREAK_LIMIT=3`, `LOW_ADOPTION_THRESHOLD=0.05`). 브랜드당
  DB 총 행수 5만 개 상한(`DEFAULT_CAP=50000`)에 닿으면 `용량상한`으로 멈춘다
  (무관 키워드가 늘어나는 것 자체는 허용하되 폭증만 막는다).

실행 진입점 `worker_main`(모듈 안 `--worker` CLI)은 브랜드 전용 복제 프로필
(`keyword_discovery_parallel`이 만든 `data/browser-profile-naver-kw-<브랜드>`)로
키워드 도구를 헤드리스로 열고, `LLMRouter.from_settings`(실행기 메인 DB를 열지
않는 경로 — 실행기 DB에 안 쓰는 규칙 준수)로 `fill_until_target`을 돈다. 로그인이
풀려 있으면 재로그인 없이 즉시 실패로 남긴다.

숨김 실행 스크립트:
[file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/scripts/keyword-fill-hidden.vbs](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/scripts/keyword-fill-hidden.vbs) →
[file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/scripts/keyword-fill-hidden.cmd](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/scripts/keyword-fill-hidden.cmd) —
브랜드 5개를 각각 숨김 파이썬 프로세스로 띄운다(CRLF·`chcp 65001`·상대경로).
로그: `logs/keyword-fill-<브랜드>.log`.

명령 배선(`키워드 채우기 시작|중지|현황`)은
[file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/command/parser.py](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/command/parser.py),
[file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/command/spec.py](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/command/spec.py),
[file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/engine/worker.py](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/engine/worker.py)에
코드로만 넣었다(실행기 재시작 금지 규칙에 따라 지금 도는 실행기에는 반영 안 됨).
**다음 `v2r serve` 재시작부터 명령으로 쓸 수 있다.** 지금 실행은 위 2번 스크립트로
직접 띄웠다.

## 3. 테스트

`.venv\Scripts\python.exe -m pytest`로 실행, 전부 통과.

- 새 테스트
  [file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/tests/test_keyword_fill_loop.py](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/tests/test_keyword_fill_loop.py)
  7건 — 네트워크 없이 가짜 도구·가짜 LLM으로 시드 선택·시드 소진 제외·채택률
  계산·용량상한·진행 파일 갱신·3연속 저채택 고갈 판정을 검증. 전부 통과.
- 기존 회귀: `test_keyword_relevance.py`(32건) · `test_keyword_discovery_parallel.py`
  · `test_naver_keyword_tool.py` 합계 49건, `test_parser.py` 75건(새 명령 3개 추가로
  `ALLOWED_TASKS` 개수 검증 57→60 갱신 포함) 모두 통과.

## 4. 첫 회차 실측 (실행 시작 00:12:40 → 이 보고서 작성 00:26)

`scripts\keyword-fill-hidden.vbs`로 5개 브랜드를 동시에 헤드리스로 띄웠다.
약 14분 경과 시점 DB 총 행수(신규 수집 누적, 아직 회차 완주 전 중간 값):

| 브랜드 | 발굴 시작 총량 | 현재 총량 | 신규 수집(이번 순환) | 채점 대기(진행 중) |
|---|---|---|---|---|
| 우아덤 | 10,000 | 13,787 | 3,787 | 3,887 |
| 장으뜸 | 10,000 | 12,699 | 2,699 | 2,399 |
| 팥순이 | 10,000 | 13,938 | 3,938 | 3,438 |
| 뉴더미스 | 10,000 | 13,500 | 3,500 | 3,100 |
| 코숨핏 | 10,000 | 11,192 | 1,192 | 892 |

(채점 대기 수가 신규 수집보다 적은 것은 그사이 일부가 이미 채점·교차검증까지
끝났기 때문 — 정확한 "채택 수·채택률"은 회차가 완주돼야 나온다. 로그에서 클로드
채점 호출이 실제로 돌고 있음을 확인했다: 예) `뉴더미스` 00:15, `코숨핏` 00:16,
`우아덤` 00:21에 채점 요청·재시도가 찍혔다. 재시도 사유는 응답 한글 자모가
살짝 달라진 것(예: "보탬알페이스"→"보탐알페이스") — 기존 재시도 로직(최대 2회)이
정상 작동 중이다.)

**회차 하나가 완주하는 데 시간이 오래 걸린다**: 브랜드당 신규 1,200–3,900개를
100개씩 묶어 클로드로 채점한 뒤 Codex(`gpt-6-astra`, low, 최대 300초/묶음)로
다시 교차검증해야 해서, 5개 브랜드 동시 실행이라도 1회차가 수십 분 이상 걸릴
것으로 보인다. 지시대로 "최소 1회차 확인 후 백그라운드로 계속 돌게 둔다" — 이
보고서 작성 시점에도 5개 프로세스가 살아서 계속 돌고 있고, `data/keywords/fill_progress.json`은
회차가 완주되는 대로(각 브랜드 독립적으로) 채워진다.

## 5. 브랜드별 1만 도달 예상 시간(현재 관측치 기준 거친 추정)

첫 회차가 아직 안 끝나 정확한 채택률을 못 구했다. 대신 **이전 발굴(BFS)** 단계의
전체 채택률(발굴 1만 개 중 원고 대상 비율)로 하한을 가늠하면:

| 브랜드 | 발굴 채택률(원고대상/발굴1만) | 남은 목표(1만-현재) | 거친 추정(같은 비율 유지 시 필요 신규 수집) |
|---|---|---|---|
| 우아덤 | 12.0% | 8,797 | 약 73,000개 |
| 장으뜸 | 2.8% | 9,717 | 약 347,000개 |
| 팥순이 | 34.5% | 6,553 | 약 19,000개 |
| 뉴더미스 | 4.7% | 9,526 | 약 203,000개 |
| 코숨핏 | 3.6% | 9,637 | 약 268,000개 |

이 추정은 **BFS 깊이가 늘수록 채택률이 떨어질 가능성**을 반영하지 못한 낙관적
하한이다(주변 낱말로 갈수록 무관해질 개연성이 큼). 장으뜸·뉴더미스·코숨핏은
5만 개 상한(`DEFAULT_CAP`)에 먼저 닿아 원고 대상 1만 개에 못 미친 채 멈출 가능성이
높다 — 정리본 논리 자체를 넓히거나(신규 제품/증상 반영) 상한을 올리는 사용자
결정이 필요할 수 있다. 팥순이는 가장 유력하게 1만 개에 도달할 것으로 보인다.

## 6. 한계·차단 리스크·대책

- **속도 병목**: 클로드 채점(100개/묶음)·Codex 교차검증(묶음당 최대 300초)이
  순차라 회차당 수십 분이 걸린다. 5개 브랜드가 각자 프로세스라 서로 막지는
  않지만, 요금제(Claude Code CLI) 세션 하나를 공유하면 한도에 먼저 닿는 브랜드가
  대기할 수 있다(`LLMRouter`가 한도 재시도를 자동 처리하므로 멈추지 않고 늦어질
  뿐이다).
- **네이버 차단**: 기존 값(조회 간 3–6초, 세션 200회당 휴식) 그대로 재사용했다 —
  새 위험을 추가하지 않았다. 다만 신규 시드가 많아질수록(정리본 보충분 포함)
  누적 조회 횟수가 늘어 장기적으로 차단 위험이 커질 수 있다.
- **5만 상한**: 일부 브랜드(장으뜸·뉴더미스·코숨핏)는 채택률이 낮아 상한에 먼저
  닿을 것으로 보인다 — 이 경우 "시드 고갈"이 아니라 "용량상한"으로 멈추므로
  구분해 살펴야 한다.
- **정리본 시드 품질**: LLM으로 뽑은 보충 시드가 부정확하면(정리본이 짧거나
  모호하면) 채택률이 더 떨어질 수 있다 — 실패 시 정규식 추출로 자동 대체하지만,
  둘 다 품질이 낮으면 회차가 반복돼도 효과가 작다.
- **실행기 명령 미반영**: `키워드 채우기 시작|중지|현황` 명령은 코드만 배선했고
  지금 도는 실행기에는 반영되지 않았다(재시작 금지 규칙) — 다음 실행기 재시작부터
  사용 가능하다. 그전까지 채우기는
  [file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/scripts/keyword-fill-hidden.vbs](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/scripts/keyword-fill-hidden.vbs)로
  수동 실행/추적한다(`data/keywords/fill_progress.json`, `logs/keyword-fill-<브랜드>.log`).
- **git 상태**: 이 저장소는 `.git`이 없어(git init 안 됨) 커밋을 시도하지 않고
  파일 작성만으로 마쳤다.


---

# 갱신 2 — 시드 다양화·척도 0–4 전환·정지/재시작 (2026-09-24 11:58, `date` 실측)

## A. 코드 변경 (커밋 299dcd3 · ba80eb4, 푸시는 저장소 규칙상 하지 않음)

- 시드 출처 5가지 — [file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/knowledge/keyword_fill_loop.py](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/knowledge/keyword_fill_loop.py) `gather_seeds`:
  (1) 원고대상(아직 시드로 안 쓴 원고 대상 상위 50), (2) 정리본(클로드, 검색창에 칠 법한 증상·상황·타깃·고민 표현 60개),
  (3) 경쟁·대체 제품명(클로드 20개), (4) 자동완성(원고 대상 상위 30개 × `naver_autocomplete_all`, 회차당 100개 상한),
  (5) 연관검색(통검 하단 "함께 많이 찾는", 헤드리스 DOM, 상위 30개 × `naver_related_searches`, 100개 상한).
  시드는 `fill_seeds` 표에 출처와 함께 기록해 두 번 쓰지 않고, DB에 이미 있는 키워드·중복은 뺀다.
  수집된 키워드는 `seed_source_type` 열로 출처를 갖고, 회차마다 출처별 신규·채택·채택률을 `fill_progress.json`의 `by_source`에 남긴다.
- 자동완성·연관검색 함수는 [file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/knowledge/keyword_exposure.py](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/knowledge/keyword_exposure.py)에 추가. 연관 검색어는 httpx HTML에는 없고(JS 렌더) 최종 DOM 링크의 `sm=tab_clk.ndT&query=`에서 뽑는다. 키워드 도구 워커 스레드 안에서는 Playwright sync 충돌이 나서 별도 스레드로 연다(실측 00:49 전부 실패 → 수정 후 76개 수집).
- 원고 대상 판정은 `keyword_relevance.is_manuscript_target`(클로드·Codex 둘 다 0에서 3, needs_review 아님, 코덱스 미채점·논리 없는 3 제외 — 커밋 57ceb0b)과 `MANUSCRIPT_MAX_RELEVANCE`를 그대로 따른다(하드코딩 없음). 재채점 대기로 `scored_at`이 비워진 행은 세지 않는다.
- 정지 수단: `data/keywords/fill_STOP` 파일을 만들면 워커가 회차 사이·조회 사이·채점/교차검증 호출에서 스스로 멈춘다(강제 종료 대체). "database is locked"는 60초 간격 최대 20회 재시도. 같은 브랜드 옛 워커가 프로필 잠금을 쥐고 있으면 120초마다 다시 시도(최대 24시간).
- 키워드 도구 입력창을 못 찾으면(시드 수집에 몇 분 걸리는 동안 페이지가 바뀜, 00:46 실측) 페이지 재진입 후 1회 재시도.
- 테스트: [file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/tests/test_keyword_fill_loop.py](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/tests/test_keyword_fill_loop.py) 15건 + `test_keyword_relevance.py` 32건 통과(가짜 도구·LLM, 네트워크 없음).

## B. 03:35 정지 원인과 재시작 (코디네이터 점검 요청)

- 장으뜸·팥순이 워커: `sqlite3.OperationalError: database is locked`로 03:35경 사망(out.log 트레이스). 실행기 쪽 재채점/시트 반영이 같은 브랜드 sqlite에 쓰던 순간과 겹쳤다. → 잠김 재시도로 보강(ba80eb4).
- 우아덤·코숨핏·뉴더미스 워커(01:17 시작, 옛 코드)는 죽지 않았고 아주 느리게 채점 중이다(재채점 대기로 돌려진 옛 "3=무관" 행 약 6,000개까지 같이 집어 100개 묶음마다 3에서 5분). 요금제 길이 240초를 넘겨 API 길로 넘어간 호출이 로그에 있다(우아덤 21회·코숨핏 7회·팥순이 2회·장으뜸 1회·뉴더미스 1회 — 비용 발생 지점).
- 옛 워커 3개는 정지 파일을 모르는 코드라 `fill_STOP`으로 끝낼 수 없다. 지시대로 강제 종료하지 않았다. 대신 새 워커가 잠금이 풀리면 이어받도록 했고, 이 세 브랜드는 옛 워커의 현재 회차가 끝나야 새 조건(57ceb0b·fadf478)으로 넘어간다.
- 10:36 재시작: 장으뜸·팥순이는 새 코드로 시작해 10:48/10:49에 시드 수집을 마쳤다(장으뜸 원고대상 50·정리본 55·경쟁 5·자동완성 8·연관검색 59, 팥순이 50·53·8·4·36). 우아덤·코숨핏·뉴더미스 새 워커는 프로세스 목록에 남아 있지 않아(로그·트레이스 없음) 원인 확인 중 — 손으로 같은 명령을 띄우면 잠금 대기 상태로 살아 있는 것은 확인했다(11:57).

## C. 첫 회차 완주 실측 — 뉴더미스(옛 워커, 01:25 시드 → 11:54 완주, 약 10시간 30분)

| 출처 | 시드 수 | 신규 수집 | 채택(원고 대상) | 채택률 |
|---|---|---|---|---|
| 원고대상 | 49 | 6,376 | 24 | 0.4% |
| 정리본 | 35 | 17 | 4 | 23.5% |
| 경쟁 | 8 | 8 | 5 | 62.5% |
| 자동완성 | 76 | 3,671 | 0 | 0.0% |
| 연관검색 | 32 | 101 | 0 | 0.0% |
| 합계 | 200 | 10,173 | 33 | 0.3% |

읽는 법: 기존 원고 대상 키워드를 시드로 넣으면 양은 많지만(키워드 도구가 시드당 최대 1,000개를 돌려줌) 거의 무관이고, 정리본·경쟁 시드는 양은 적어도 채택률이 높다. 자동완성·연관검색은 이번 회차엔 채택이 0 — 다만 이 회차의 채택은 Codex 교차검증이 끝난 것만 세므로(엄격 판정) 교차검증 적체가 풀리면 올라갈 수 있다. 회차가 10시간 넘게 걸린 이유는 옛 무관 행 재채점 + 100개 묶음 오타 재시도(fadf478로 50개 묶음·오타 수용 반영, 옛 워커에는 미적용).

## D. 브랜드별 원고 대상 수 (엄격 판정, 11:55 실측) 와 적체

| 브랜드 | 원고 대상 | DB 총량 | 클로드 채점 대기 | Codex 교차검증 대기 | 채점 분포(0/1/2/3/4) |
|---|---|---|---|---|---|
| 우아덤 | 1,203 | 26,705 | 7,933 | 15,305 | 73/1,648/5,576/215/11,260 |
| 장으뜸 | 283 | 33,412 | 25,924 | 6,059 | 78/443/2,005/292/4,670 |
| 팥순이 | 3,447 | 36,831 | 24,724 | 6,633 | 237/2,992/4,442/386/4,050 |
| 뉴더미스 | 602 | 27,539 | 7,045 | 1,450 | 147/358/1,668/1,104/17,217 |
| 코숨핏 | 461 | 28,118 | 7,768 | 10,178 | 202/496/2,605/533/16,514 |

- 원고 대상이 처음(00:12) 값에서 거의 늘지 않은 이유: 엄격 판정이 Codex 교차검증을 요구하는데 교차검증 대기가 브랜드당 1,450에서 15,305건 쌓여 있다. 클로드 채점(0에서 3)만 끝난 행은 아직 안 센다. 병목은 Codex 교차검증(묶음당 최대 300초)이다.
- 10:54부터 실행기 쪽 재채점 워커(rescore-worker 10개)가 같은 DB를 쓰고 있어 잠김 충돌 가능성이 있다(재시도로 흡수).
- 수집 총량은 이미 브랜드당 26,705에서 36,831로 5만 상한의 절반을 넘었다. 채택률 0.3%면 5만 상한에서 멈추고, 1만 도달은 이 방식으로는 불가능하다 — 정리본·경쟁 출처 비중을 키우고 원고대상·자동완성 시드는 줄이는 조정이 필요하다(코드 상수 `DEFAULT_SEED_LIMIT`·`DEFAULT_EXPAND_SEED_CAP`).

## E. 12:12 추가 실측 — 사라진 워커의 진짜 원인과 현재 워커 목록 (커밋 6a487f7 · 6693daa · 0f07eef)

- 원인: `scripts\keyword-fill-hidden.cmd`가 `start /B ... >> logs\keyword-fill-<브랜드>.out.log`로 띄우는데, 같은 브랜드의 옛 워커가 그 파일을 이미 `>>`로 쥐고 있으면 cmd가 파일을 못 열어 새 워커가 **아무 출력 없이 exit 1**로 사라진다(같은 명령을 새 파일로 돌리면 정상 대기 — 12:05에서 12:10 사이 6회 재현·확인). 장으뜸·팥순이는 옛 워커가 이미 죽어 파일이 비어 있었기에 살아남았다.
- 조치: 워커마다 [file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/scripts/keyword-fill-worker.vbs](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/scripts/keyword-fill-worker.vbs)로 자기 숨김 콘솔에서 띄우고, 표준출력은 띄울 때마다 새 파일(`logs\keyword-fill-<브랜드>.<시각>.out.log`)에 쓴다. `keyword-fill-hidden.cmd`는 이 VBS를 5번 부른다.
- 현재 워커(12:12): 옛 코드(01:17 시작, 정지 파일 무시) 우아덤·코숨핏·뉴더미스 각 1개가 프로필 잠금을 쥐고 채점 중 / 새 코드 장으뜸·팥순이(10:36) 회차 진행 중 / 새 코드 대기 워커(잠금 풀리면 이어받음) 우아덤 12:06, 코숨핏 12:07, 뉴더미스 12:10. 진단 중 띄운 여분 대기 워커(코숨핏 11:58 — 15분 뒤 자동 종료)는 잠금 순서만 기다릴 뿐 DB를 건드리지 않는다.
- 옛 워커 3개는 지시대로 강제 종료하지 않았다. 이들이 현재 회차를 마치는 시점(뉴더미스 사례로 회차당 약 10시간 30분)부터 새 조건(57ceb0b 엄격 판정·fadf478 50개 묶음)이 적용된다. 즉시 전환이 필요하면 사용자 결정 후 pid 10368(우아덤)·16040(코숨핏)·37956(뉴더미스) 종료가 필요하다.

## F. 시드 비중 조정과 브랜드별 1만 개 도달 현실 전망 (12:15 실측, 커밋 반영)

시드 상수 변경(코디네이터 지시 12:20): 정리본 60→150, 경쟁 20→60, 원고대상 50→10, 자동완성·연관검색 각 100→10
([file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/knowledge/keyword_fill_loop.py](file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/v2r-command-system/v2r/knowledge/keyword_fill_loop.py) `DEFAULT_SEED_LIMIT`·`DEFAULT_GUIDE_SEED_N`·`DEFAULT_COMPETITOR_SEED_N`·`DEFAULT_EXPAND_SEED_CAP`).
옛 워커 3개(우아덤 pid 10368·코숨핏 16040·뉴더미스 37956)는 강제 종료하지 않았다. 새 상수를 읽은 대기 워커(우아덤 12:06·코숨핏 12:07·뉴더미스 12:10 시작)가 프로필 잠금이 풀리는 즉시 이어받으며, 이어받은 시각은 그 브랜드 로그의 "브라우저 열림" 줄과 `fill_progress.json`의 다음 회차 `updated_at`으로 남는다(12:15 현재 아직 옛 워커가 잠금 보유 중 — 세 브랜드 모두 "옛 워커가 프로필을 쓰는 중, 120초 뒤 다시 시도" 반복).

전망 계산 근거:
- 재채점 증가분 = 채점 대기 행 × 오늘 재채점 결과의 0에서 3 비율(10:54 이후 새 프롬프트로 채점된 행 1,900에서 3,590건 기준 — "첫 500개"보다 큰 표본). Codex 교차검증이 같은 판정을 내린다고 가정한 상한이며, 실제로는 교차검증에서 일부가 빠진다.
- 회차당 채택 수 = 뉴더미스 1회차 출처별 실측(정리본 시드 35개→채택 4, 경쟁 8개→5, 원고대상 49개→24, 자동완성 76개→0)을 새 시드 비중(150·60·10·10)에 비례시킨 값 ≈ 정리본 17 + 경쟁 38 + 원고대상 5 + 자동완성 0 ≈ 60건/회차, 신규 수집 약 1,850건/회차.
- 회차 시간: 옛 무관 행 재채점 적체가 빠지면 신규 1,850건을 50개 묶음으로 채점·교차검증하는 데 약 4시간(하루 5에서 6회차, 약 300에서 360건/일)으로 추정. 다만 브랜드당 5만 행 상한까지 남은 여유가 회차 수를 먼저 제한한다.

| 브랜드 | 지금 확정(엄격) | 재채점 0에서 3 비율(표본) | 채점 대기 | 재채점으로 늘어날 수(상한) | 회차당 채택 | 5만 상한까지 남은 회차 | 이 방식의 도달 상한 | 판정 |
|---|---|---|---|---|---|---|---|---|
| 우아덤 | 1,203 | 31.6% (2,773건) | 7,475 | +2,361 | 약 60 | 12 | 약 4,300 | 이 방식으론 불가 · 상한 약 4,300 |
| 장으뜸 | 283 | 25.5% (3,590건) | 25,250 | +6,435 | 약 60 | 8 | 약 7,200 | 이 방식으론 불가 · 상한 약 7,200 |
| 팥순이 | 3,447 | 47.6% (3,123건) | 23,923 | +11,390 | 약 60 | 7 | 약 15,300 | 도달 가능 — 재채점 적체 해소 시점(현재 속도로 약 3에서 4일)에 1만 넘김, 순환 기여는 작음 |
| 뉴더미스 | 602 | 14.8% (2,300건) | 11,143 | +1,652 | 약 60 | 9 | 약 2,800 | 이 방식으론 불가 · 상한 약 2,800 |
| 코숨핏 | 465 | 21.9% (1,900건) | 7,318 | +1,602 | 약 60 | 11 | 약 2,700 | 이 방식으론 불가 · 상한 약 2,700 |

읽는 법: 팥순이만 재채점만으로 1만을 넘길 수 있고, 나머지 네 브랜드는 재채점 증가분을 다 더해도 2,700에서 7,200에서 멈춘다. 채우기 순환은 회차당 약 60건이라 5만 상한 안에서 기여가 400에서 700건에 그친다. 1만에 닿으려면 (1) 브랜드당 상한을 올리고 무관 행을 주기적으로 지워 자리를 비우거나, (2) 정리본·경쟁처럼 채택률이 높은 시드를 회차마다 새로 만들어 내는 공급원(예: 원고 대상 키워드에서 LLM으로 파생 검색어 생성)을 더해야 한다. 두 가지 모두 사용자 결정 사항이다.

### F-1. 상수 반영을 위한 자체 정지·재기동 (12:15에서 12:21)

시드 상수는 모듈을 읽을 때 정해지므로 12:06에서 12:10에 띄운 대기 워커와 10:36 시작 장으뜸·팥순이는 옛 값(50·60·20·100)을 갖고 있었다. 강제 종료 대신 정지 파일(`data/keywords/fill_STOP`)을 12:15에 만들자 새 코드 워커 4개가 12:17에서 12:20 사이 스스로 멈췄고(장으뜸·팥순이는 채점 단계에서 남은 묶음을 건너뛰고 회차를 닫음 — 이미 저장된 점수는 유지), 정지 파일을 지운 뒤 12:20에 5개 브랜드를 새 상수로 다시 띄웠다(우아덤 pid 32928·코숨핏 34684·뉴더미스 30164·장으뜸 24212·팥순이 15972). 옛 워커 3개(01:17)는 여전히 잠금을 쥐고 있어 우아덤·코숨핏·뉴더미스 새 워커는 120초 간격으로 대기 중이며, 이어받은 시각은 잠금이 풀린 뒤 로그 "브라우저 열림" 줄로 남는다(이 보고서 작성 시점엔 아직 없음).

정지 전 장으뜸·팥순이 1회차(옛 비중)는 채점이 끝나지 않아 채택 수가 0으로 닫혔고 참고값만 남았다: 장으뜸 신규 989(경쟁 시드 5개에서 539건 — 경쟁 시드가 키워드 도구에서 가장 많은 연관어를 돌려준 사례), 팥순이 신규 2,695(원고대상 2,524·정리본 128).

## G. 남은 일

- Codex 교차검증 적체(브랜드당 2,300에서 15,763건)가 풀리지 않으면 엄격 판정 원고 대상 수는 늘지 않는다 — 교차검증 병렬화 또는 묶음 축소 검토.
- 옛 워커 3개가 잠금을 놓는 시각 기록(로그 "브라우저 열림" 줄) 후 새 비중의 첫 회차 채택률로 F절 수치를 재검증.
- 푸시는 저장소 규칙(원격 push 금지)에 따라 하지 않았다 — 커밋만 남겼다.
