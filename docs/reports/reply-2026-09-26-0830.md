# 답변 (09-26 08:30) — 3건 착수

## 1. 노출 확인 속도 병목 점검·최적화 (진행 중)
- 오늘 로그로 검사 1건당 소요를 단계별(검색 요청 · 고정 지연 · 대기열/DB · 시트 반영 · 브라우저 재기동)로 실측 분해하고, 근거 있는 것만 바꾼다: 시트 CSV 읽기 캐시, 고정 지연 3에서 5초 → 2에서 4초(차단 징후 시 자동 복귀), 작업자 6 → 8(차단 징후 없을 때만), 검색 타임아웃·재시도 조정.
- 각 변경마다 30분 실측(시간당 건수·중복률·차단 징후)으로 확인하고 효과 없으면 되돌린다. 목표 시간당 800건 이상.
- 결과 파일: docs/reports/exposure-speed-2026-09-26.md (완료 시 링크 전달).

## 2. 키워드 1만 개 진행률 개선 (진행 중)
- 어제 회차 확정률 2.5%의 원인을 시드 출처별(정리본·경쟁·파생·자동완성·연관검색·원고대상) 확정률 표로 실측.
- 바꾸는 것: 확정 키워드(0에서 2, 검색량순, 미사용) 우선 시드 · 출처별 예산을 최근 확정률에 비례 자동 조정 · 확정 키워드의 자동완성·연관검색 2단계 수집 · 채점 적체 5,000 이상이면 수집 일시 정지 · 거부 키워드 재수집 차단.
- 적용 후 첫 회차를 1시간 안에 실측해 2.5%와 비교. 결과 파일: docs/reports/keyword-fill-optimization-2026-09-26.md.

## 3. 새 카테고리 "갱년기" 1만 개 (착수)
- 기존 브랜드와 같은 파이프라인으로 추가: 설정(brands·sources·정리본 요약·시드 60개 이상·키워드 DB), 채우기 순환 + 채점·2차 판정 워커 시작. 노출 확인 대상에는 넣지 않음(발행 글 없음).
- 시트(1syyaWQz…)는 Apps Script 허용 목록에 추가해 두며, **시트에 키워드를 붙이려면 Apps Script 재배포 1회**가 필요하다(코드 준비되면 복사 페이지 갱신 후 안내). 수집·채점은 배포와 무관하게 지금 진행.
- 30분 실측 뒤 결과 파일: docs/reports/menopause-keywords-2026-09-26.md.

세 작업 모두 끝나는 대로 각 파일 링크를 한 줄씩 보낸다.
