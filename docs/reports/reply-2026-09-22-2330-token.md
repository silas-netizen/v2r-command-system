# 토큰 입력은 제가 못 합니다 — 직접 30초 (2026-09-22 23:25 KST, 실측)

- 규칙: 비밀번호·API 키·토큰은 **제가 입력·복사·저장하지 않습니다**(사용자 요청이 있어도 예외 없음, 시스템이 차단). 로그인 철칙과 같은 층위의 보안 규칙입니다.
- 그래서 남은 건 딱 이것뿐입니다:
  1. 열어 둔 크롬 창(Installed App Settings) → **Copy** 클릭.
  2. 탐색기에서 `D:\v2r 자동화\v2r-command-system\.env` 를 메모장으로 열기 → 아래 두 줄로 고치고 저장.
     ```
     SLACK_BOT_TOKEN=(붙여넣기)
     SLACK_ALLOWED_CHANNEL_IDS=C0C3GD9CA05
     ```
  3. "슬랙 넣었어"라고 하시면 재시작 → `슬랙 점검`(메시지·파일 전송 확인) → 결과 파일.
- 나머지(앱·권한·설치·채널 초대·채널 ID)는 전부 끝나 있습니다.
