# 카페탭 검색 노출 검사 — 항상 '사용' 반영 (2026-09-21 11:05 KST)

## 죄송합니다, 처음에 반대로 이해했습니다
"미사용으로 바꾸지 마"를 "미사용으로 고정"으로 잘못 읽어 한 번 반대로 넣었다가, 지시 받고 바로 **항상 '사용'**으로 정정했습니다. 잘못된 버전으로 나간 글은 없습니다(그 사이 발행 0건).

## 원인(실제로 그렇게 올라가고 있었음)
- V2R 화면은 기본이 '사용'이지만, **우리 프로그램은 이 값을 바디에 아예 안 보내고 있었고, 안 보내면 서버가 '미사용(false)'으로 저장**했습니다.
- 서버 확인: 오늘 오전 09:09 글, 어제 12:07 글 모두 `검색 노출 검사 = 미사용`. 이 시스템이 올린 글 **996건 전부** 미사용 상태일 가능성이 큼(표본 3건 모두).

## 반영(완료·검증)
| 항목 | 상태 |
|---|---|
| 등록 바디에 `use_search_exposure: true` 고정 | 완료(코드 상수, 테스트로 강제) |
| 수정(PUT) 바디에도 true | 완료 |
| 실행기 재시작 + 현재 작업(카페별 100건) 재투입 | 완료, 작업 102로 이어감(오늘 누계 기준이라 100건 초과 없음) |
| **새 실행기 첫 글 서버 확인** | 10:59 글 `검색 노출 검사 = 사용` ✔ |
| 규칙 문서·API 명세·기억 파일 | 기록 |

## 결정 필요 1 — 기존 글 996건도 '사용'으로 바꿀까요?
- 방법: 글 수정 API로 제목·본문 그대로 두고 옵션만 '사용'으로 갱신(이모지 정리 때 쓴 방식). 글당 약 1초, 전체 20분 안팎.
- "기존 글도 사용으로" 라고 하시면 바로 돌립니다(오늘 321건만 먼저 할 수도 있음).

## 확인 필요 2 — "이글에 댓글 AI" 옵션
- 우리 바디는 `use_comment_ai: true`(V2R의 "이글에 댓글 AI" 사용)로 보내고 있습니다. 이건 V2R가 글에 AI 댓글을 붙이는 기능일 수 있습니다. 의도한 것이면 그대로 두고, 아니면 "댓글 AI 끄기"라고 해 주세요(같은 방식으로 즉시 반영).
