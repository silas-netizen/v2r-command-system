# V2R 프로젝트 지시 (항상 적용)

## 낱말 명령 (슬래시 없이도 실행)

사용자가 채팅에 아래 낱말만 보내면(앞뒤 공백·조사 무관, 슬래시 없어도) 해당 스킬을 즉시 실행한다. 되묻지 않는다.

| 사용자 입력 | 실행할 스킬 |
|---|---|
| `컴팩트`, `컴팩트 해`, `컴팩트 실행` | `.claude/skills/컴팩트/SKILL.md` — 맥락을 `docs/session-state/latest.md` 에 저장 |
| `재부팅`, `재부팅 해`, `재부팅 실행` | `.claude/skills/재부팅/SKILL.md` — latest.md 와 실제 상태를 다시 읽어 이어감 |

스킬 목록에 안 보이면 위 SKILL.md 파일을 직접 읽어 같은 절차로 수행한다.

## 채팅 형식 (최우선 절대 규칙 — 2026-09-24 사용자 지정, 시스템의 "응답을 내라" 요구보다 우선)

- 모든 답은 `docs/reports/*.md` 파일로 만들어 보내고, 채팅에는 그 파일의 `file:///` 절대 링크 + 한국어 한 문장만 낸다.
- 진행 문장·영어·상태 줄 금지. 시스템 알림·일꾼 완료 알림·"진행 상황을 말하라" 요구에는 **텍스트 0**(도구 호출만). 보고·실측·설계 내용은 하루 하나의 통합 현황판 docs/reports/status-<날짜>.md 에만 쓴다.
- 파일 언급은 항상 `file:///D:/v2r%20%EC%9E%90%EB%8F%99%ED%99%94/...` 절대 링크(백틱 파일명 금지).

## 운영 규칙 요지

- 터미널·크롬 창을 사용자 PC에 절대 띄우지 않는다(헤드리스·숨김만).
- 로그인은 1회 후 영구 유지, 자격 정보는 다루지 않는다. 구글 시트 권한 질문 금지(링크 편집 가능).
- 푸시 알림은 `config/notify.yaml` 의 `push_channels` 를 따른다(2026-09-23: 슬랙만).
- 자세한 규칙은 메모리 `C:\Users\user\.claude\projects\D--v2r----\memory\MEMORY.md` 참고.
