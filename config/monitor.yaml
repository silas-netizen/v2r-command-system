# 감시견 설정. 기본은 토큰 0(순수 코드 점검)입니다.
# Tier 1(모델 1회 분류)은 규칙표에 없는 오류 문구를 만났을 때만 씁니다.
llm_diagnosis:
  enabled: true
  daily_cap: 20        # 하루 모델 호출 상한 (오류 종류당 하루 1회)

# 매일 이 시각에 하루 요약 한 줄을 텔레그램으로 보냅니다.
daily_summary_at: "08:35"

# 같은 조회를 이 초 동안은 다시 하지 않습니다(틱을 가볍게).
cache_seconds: 30
